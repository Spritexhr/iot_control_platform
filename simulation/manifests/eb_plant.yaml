# 乙苯(EB)装置清单 —— 占位
#
# 当前 simulation/devices/eb_plant/eb_plant_simulator.py 是装置级单进程模拟器，
# 不属于 REGISTRY 模式。等 EB 设备按反应器/蒸汽包/DEB 塔/泵阀拆分成独立 MqttNode
# 之后，把它们填进 nodes 列表，就能用：
#
#   python run.py --manifest eb_plant
#
# 单独启动整套 EB 装置。
#
# 当前阶段如果要跑老的装置级模拟器，仍然直接：
#   python devices/eb_plant/eb_plant_simulator.py --broker <host>

name: eb_plant
description: 乙苯(EB)装置 —— 反应器/蒸汽包/DEB 塔/泵阀（待拆分填入）

nodes: []
